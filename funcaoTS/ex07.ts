// 7. Elabore uma função chamada "media" que receba três notas como parâmetros e retorne a média
// aritmética delas.

function media(n1: number, n2: number, n3: number) {
    return (n1 + n2 + n3) / 3;
}

let mediaResultado = media(10, 3, 9);
console.log(mediaResultado);